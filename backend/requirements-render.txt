# Render-specific requirements (API server only - no scraping)
# This file excludes Playwright and other heavy scraping dependencies

# Core utilities (for reading existing data)
requests
beautifulsoup4
lxml
pyyaml
openpyxl
python-dotenv

# Data processing
pandas

# Firebase/Firestore (enterprise data store)
firebase-admin>=6.2.0

# API server (for frontend integration)
flask
flask-cors
gunicorn

# Performance & monitoring
tqdm>=4.66.0
psutil>=5.9.0

# Scheduling & WebSocket support
apscheduler>=3.10.0
flask-socketio>=5.3.0
